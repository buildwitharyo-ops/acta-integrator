NEXT_PUBLIC_SITE_URL=            # https://... — dipakai canonical, sitemap, OG
NEXT_PUBLIC_SUPABASE_URL=        # Supabase situs — satu project melayani dev & prod (owner decision)
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=       # server-only: seeding & operasi admin tertentu
ACTA_OS_SUPABASE_URL=            # forward leads (server-only)
ACTA_OS_SUPABASE_SERVICE_KEY=    # server-only, JANGAN prefix NEXT_PUBLIC
DRAFT_MODE_SECRET=
CRON_SECRET=                     # proteksi /api/cron/publish-scheduled (Vercel Cron header)
NEXT_PUBLIC_GA4_ID=
NEXT_PUBLIC_LINKEDIN_PARTNER_ID=
PLAYWRIGHT_BASE_URL=              # default http://localhost:3000 — target for `npm run test:e2e`
PLAYWRIGHT_TEST_ADMIN_EMAIL=      # dedicated editor-role admin_users account, test-only
PLAYWRIGHT_TEST_ADMIN_PASSWORD=
